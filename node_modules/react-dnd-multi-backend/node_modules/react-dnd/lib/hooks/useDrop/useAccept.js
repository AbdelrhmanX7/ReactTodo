import { useMemo } from 'react';
export function useAccept(spec) {
    const specAccept = spec.accept;
    return useMemo(() => (Array.isArray(specAccept) ? specAccept : [specAccept]), [specAccept]);
}
